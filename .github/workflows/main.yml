name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python Fix-it-bot.py
Invalid workflow file : .github/workflows/main.yml
A sequence was not expected

      - run: pip install -r requirements.txt
      - run: python Fix-it-bot.py
Invalid workflow file : .github/workflows/main.yml
A sequence was not expected
name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python Fix-it-bot.py
